import { styled } from "../../../stitches.config";

export const Container = styled('section', {
    backgroundColor: '$roxinho',
    height: '130px',
    padding: '0 2rem',

    display: 'flex',
    alignItems: 'center'
})